import _ from 'underscore'

const state = {
}

const getters = {
    //locationStatus: state => state.locationStatus,
}

const actions = {
}

const mutations = {
}

export default {
    state,
    getters,
    actions,
    mutations
}
